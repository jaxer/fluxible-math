apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fluxible-math.fullname" . }}
  labels:
    {{- include "fluxible-math.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
