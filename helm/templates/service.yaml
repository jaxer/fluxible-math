apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluxible-math.fullname" . }}
  labels:
    {{- include "fluxible-math.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "fluxible-math.selectorLabels" . | nindent 4 }}
