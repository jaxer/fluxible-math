image:
  repository: ghcr.io/jaxer/fluxible-math
  tag: 5344539f5cfd3d428d3b70236595b4676150f2d6
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  port: 3000

env:
  NODE_ENV: production

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

ingress:
  enabled: true
  host: math.sander.ee
  entryPoints:
    - websecure
  tls: true

probes:
  liveness:
    path: /health
    initialDelaySeconds: 10
    periodSeconds: 10
  readiness:
    path: /health
    initialDelaySeconds: 5
    periodSeconds: 5
  startup:
    path: /health
    periodSeconds: 5
    failureThreshold: 30
